# actionlint configuration
matcher:
  SC2086: false  # Double quote to prevent globbing - not applicable in our network check context
ignore:
  - integration.yml  # Uses secrets context - out of scope for this repo
